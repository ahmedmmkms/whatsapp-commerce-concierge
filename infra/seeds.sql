-- Author: AMM
-- Minimal seed for local dev
-- Run manually against Postgres if needed.

-- Example schema placeholders (implement proper migrations later):
-- CREATE TABLE IF NOT EXISTS product (
--   id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
--   sku TEXT UNIQUE,
--   name TEXT NOT NULL,
--   price_cents INT NOT NULL,
--   currency TEXT NOT NULL DEFAULT 'USD',
--   media_url TEXT
-- );

-- INSERT INTO product (sku, name, price_cents, currency, media_url) VALUES
-- ('SKU-001', 'Basic Tee', 2500, 'USD', NULL),
-- ('SKU-002', 'Running Shoe', 7900, 'USD', NULL),
-- ('SKU-003', 'Cap', 1500, 'USD', NULL);

